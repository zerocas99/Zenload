FROM aiogram/telegram-bot-api:latest

# The image already has everything configured
# Just need to pass environment variables:
# - TELEGRAM_API_ID
# - TELEGRAM_API_HASH
# - TELEGRAM_LOCAL=1 (for local mode - allows file paths)

ENV TELEGRAM_LOCAL=1

EXPOSE 8081
